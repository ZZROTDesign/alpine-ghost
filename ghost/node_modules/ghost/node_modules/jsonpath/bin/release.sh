# bump version in package.json
perl -pi -e 's/"version": ".+"/"version": "$VERSION"/' package.json

# rebuild generated js + check in
npm install
node_modules/.bin/grunt-cli

# add new tag
git tag $VERSION
git push
git push --tags

# npm publish
npm adduser
npm publish

